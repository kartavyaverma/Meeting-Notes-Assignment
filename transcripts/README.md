# transcripts

Put call transcripts here as plain `.txt` files (Zoom/Teams or Otter exports).

Only files named `sample-*` are committed to git. Everything else in this folder is ignored, because real transcripts contain client data.
