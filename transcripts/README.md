# transcripts

Put call transcripts here as plain `.txt` files.

Everything in this folder is git-ignored **except** files named `sample-*`. Name a file `sample-...` only if it's safe to share, meaning it's fictional or has client details removed.
